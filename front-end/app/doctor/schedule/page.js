import SchedulePicker from "../../_components/SchedulePicker";

export default function SchedulePage() {
  return (
    // A simple container for the page
    <div className="bg-white min-h-screen flex items-center justify-center">
      <SchedulePicker />
    </div>
  );
}